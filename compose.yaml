services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: tsilva
      POSTGRES_PASSWORD: b1b1
      POSTGRES_DB: aiops
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
